BASTION_IP=$(terraform output -raw bastion_publicip)

cat > inventory.ini <<EOL
[bastion]
bastion ansible_host=$BASTION_IP ansible_user=ubuntu ansible_ssh_private_key_file=/home/mokchhedul/secondaccount.pem
EOL